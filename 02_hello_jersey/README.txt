============================================
  Hello World Codelab - Hello Jersey
============================================

Task for this step:

  Can you add a new output MIME type that
  is used by the Hello resource class?


For Example:

  - Add a custom XML format
  
Extra Credit:

  - Can you make the HelloResource return a different message based
    on the HTTP client 'Accept-Language' header? (Hint: look at
    HttpHeaders.getAcceptableLanguages())
